<div class="allBeers">
    {{#each beersFromApi}}
    <div><img src={{this.image_url}}></div>
    <div>{{this.name}}</div>
    <div>{{this.description}}</div>
    <div>{{this.tagline}}</div>

    {{/each}}
</div>